# 京漫便民 Windows 人工验收版

本机已安装腾讯官方微信开发者工具2.02.2608070；本目录包含可导入的15页小程序、商家网页、模拟后端和独立Python环境。来源提交 c87425f，本地增加Windows适配、支付接入边界、可切换收银模拟场景和验收台。

1. 双击 `start.cmd`：后台启动两个本机服务，并打开人工验收网页。
2. 微信开发者工具导入本目录的 `miniprogram` 文件夹；选择你有权限的AppID，后端选不使用云服务。
3. 小程序连接页地址 `http://127.0.0.1:8874`，口令从验收网页复制。选择顾客或店员进入。
4. 按网页逐项操作、记录结果与问题；最后导出验收JSON。
5. 双击 `stop.cmd` 可停止本次拥有的服务，保留数据。下次启动不重置订单。

模拟后端8874；收银签名协议模拟8873。程序只监听电脑本机；无需商户密钥，不发生真实付款。数据、模拟密钥及运行日志保存在 `%LOCALAPPDATA%\JingmanAcceptance`，不放在小程序工程中。

退款和打印队列使用手动验收模式：店长进入“故障演练/对账”点击“重试任务”才处理，便于复现失败。测试后请关闭故障开关，并恢复收银场景为正常。

源代码中游客AppID是占位；个人AppID可保存在本机 `project.private.config.json`，不要提交AppSecret。微信登录由你本人扫码完成。新增代码会触发开发工具编译；若未刷新可手动点“编译”。

Windows 首次重建环境可运行 `setup-windows.cmd`（需已安装Python3.10+并联网）；当前电脑已准备好 `.venv`，可直接启动。`.venv`依赖本电脑的Python基础运行时，整个目录不是任意电脑免安装包。

真实接入所需资料与开发位置见 `docs/真实接入清单.md`，验收网页也提供同内容入口。营销、完整商品新增/改价、门店设置和导出目前在网页后台；流程表明确注明操作端。
