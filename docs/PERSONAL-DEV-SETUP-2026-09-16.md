# 个人小程序本机测试已配置（2026-09-16）

## 现在打开哪个工程

开发工具项目名：`jingman-personal-test`。

工程目录：`/home/vimalinx/MakeMoney/SuperMarket/jingman-devtools-personal-20260916`。

当前已经登录实验室顾客“小雨”，可以在开发工具右侧模拟器浏览首页商品。工程使用本人登录账号在开发工具“其他可用 AppID”中列出的项目，基础库固定为本机可运行的 `3.17.3`。个人 AppID 只放在这个独立工程配置；没有将 AppSecret 或实验室口令写入源代码。原游客工程未覆盖。

![个人工程实际显示首页与本地商品](../output/verification/mock-20260915/setup-personal-home.png)

截图背景旧窗口中的二维码不是小程序体验码，不要用它作为手机打开入口。

## 下次启动

先确认本地模拟服务仍在运行。已经运行时无需重复启动；停止后，在终端运行：

```bash
bash /home/vimalinx/MakeMoney/SuperMarket/jingman-lab/tools/mock-retail.sh
```

再在微信开发者工具打开上述工程目录。登录页地址保持 `http://127.0.0.1:8874`，使用启动终端显示的实验室口令，不是微信密码；选择“顾客·小雨”进入首页，或选择“店长”检查工作台。服务端生成的密钥和演练数据留在 `/home/vimalinx/.local/state/jingman-local-mock`，不要复制进工程或发给商家。

若服务提示端口已占用，不要杀掉未知进程；先确认是不是已有模拟服务。修改业务代码应改 `/home/vimalinx/MakeMoney/SuperMarket/jingman-lab/miniprogram`，本测试目录是导出副本，不会自动跟随源码更新。

## 本次实际验证

- 从已登录开发工具取得账号可用 AppID，导入独立工程并编译到连接页。
- 使用本地实验室口令进入顾客首页，实际显示商品、图片及演练提示。
- 点击底部“分类”，实际显示16条演练商品、价格及可售数量；截图为 `../output/verification/mock-20260915/setup-personal-category.png`。
- 对比源码和导出目录，75个非环境配置文件全部一致。
- 控制台仍提示本地 HTTP 图片应迁移 HTTPS，以及本地关闭域名校验相关警告。图片目前在模拟器可见；这些警告没有被隐藏，也不算正式环境验收通过。

本次没有重跑全量回归，没有把旧游客工程测试结果当作个人 AppID 全页面验收。原商家数据库和真实拉卡拉均未写入。

## 仍不能当正式小程序使用

这次完成的是开发工具内运行，不是手机体验版或审核发布。未点击预览、上传、发布，也未开通云开发。正式微信用户认证、员工授权、支付和真实拉卡拉同步仍未接入。

手机上的 `127.0.0.1` 指手机自己，当前只能电脑内联调。下一阶段需先配置受访问控制保护、手机可达的测试后端及图片地址，再在授权范围内生成预览码并做手机验收；不能直接把允许任意演练身份切换的实验室暴露公网。
